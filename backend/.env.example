PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/routewise
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development

# Geographic Provider Configuration (OSM MVP)
# A User-Agent is required by OSM Nominatim/Overpass terms of service
GEO_USER_AGENT=RouteWise-MVP/1.0 (dev@localhost)
# Optional: Point to a custom OSRM instance (defaults to public demo server)
# GEO_OSRM_URL=http://localhost:5000
